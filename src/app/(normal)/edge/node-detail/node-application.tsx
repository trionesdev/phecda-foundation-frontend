import { FC } from 'react';

type NodeApplicationProps = {
    node: any;
};
const NodeApplication: FC<NodeApplicationProps> = ({ node }) => {
    return <div>节点应用，TODO</div>;
};
export default NodeApplication;
