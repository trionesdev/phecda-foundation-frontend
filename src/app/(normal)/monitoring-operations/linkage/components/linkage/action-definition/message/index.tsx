import { Space } from 'antd';
import { FC } from 'react';

type MessageActionProps = {
    namePath?: any[];
};
export const MessageAction: FC<MessageActionProps> = ({ namePath = [] }) => {
    return <Space>ss</Space>;
};
